// Implicit typing
var implicitVariable = 50; // Compiler infers the type based on the assigned value (int in this case)
Console.WriteLine("Implicit Variable " + implicitVariable);

// Explicit typing
int explicitVariable = 20; // Type is explicitly specified as int
Console.WriteLine("Explicit Variable " + explicitVariable);
